import { IncludeLabelPipe } from './include-label.pipe';

describe('IncludeLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new IncludeLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
