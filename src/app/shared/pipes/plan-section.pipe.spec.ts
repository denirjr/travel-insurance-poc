import { PlanSectionPipe } from './plan-section.pipe';

describe('PlanSectionPipe', () => {
  it('create an instance', () => {
    const pipe = new PlanSectionPipe();
    expect(pipe).toBeTruthy();
  });
});
